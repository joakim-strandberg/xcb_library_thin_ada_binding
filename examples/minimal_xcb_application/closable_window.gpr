with "../../xcb";

project Closable_Window is
    for Source_Dirs use ("src");
    for Object_Dir use "obj";
    for Main use ("main.adb");

    package Linker is
       for Default_Switches ("Ada") use ("-L/opt/X11/lib/");
    end Linker;

end Closable_Window;
